# Environment Variables for Railway Backend Service
# Copy these to your Railway backend service settings

# Database Connection (Reference Postgres service)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# Redis Connection
# Use the environment variables that Railway provides from the Redis service
REDIS_URL=redis://default:${{REDIS_PASSWORD}}@${{REDISHOST}}:6379
# Note: REDIS_PASSWORD and REDISHOST are automatically provided by Railway

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS Configuration (Update after Vercel deployment)
CORS_ORIGIN=https://your-app.vercel.app

# Optional: Cache TTL in seconds
CACHE_TTL=300
